"use client";

import React, { FC } from "react";
import { SettingsPage } from "@shesha/reactjs";

const Page: FC = (props) => {
  return <SettingsPage {...props} />;
};

export default Page;
