import React, { FC } from 'react';
import { MainLayout } from '@shesha/reactjs';

interface IProps {}

const MaintenancePage: FC<IProps> = () => {
  return (
    <MainLayout title={`Maintenance`}>
    </MainLayout>
  );
};

export default MaintenancePage;
