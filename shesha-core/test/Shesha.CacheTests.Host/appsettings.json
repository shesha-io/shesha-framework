{
  "//": "Baseline configuration for the cache-test host. Every value below is overridden per instance by the provisioner (Testcontainers) or by docker-compose, including the connection string and the JWT signing key.",

  "ConnectionStrings": {
    "Default": "Data Source=sql,1433;Initial Catalog=SheshaFunctionalTests;User Id=sa;Password=Cache@Test1;TrustServerCertificate=True;Encrypt=False;"
  },
  "DbmsType": "SQLServer",
  "IsAzureEnvironment": false,

  "App": {
    "ServerRootAddress": "http://localhost:8080",
    "CorsOrigins": "http://localhost:3000"
  },

  "//Authentication": "The signing key MUST be identical across all instances or a token minted by one will be rejected by the others. The suite asserts this, so drift here would otherwise look like cache incoherence.",
  "Authentication": {
    "JwtBearer": {
      "IsEnabled": "true",
      "SecurityKey": "ZQJK9FR81M9NDCKX161B2L9RXDBHL7GS",
      "Issuer": "Shesha",
      "Audience": "Shesha"
    }
  },

  "SheshaApp": {
    "HomeUrl": "/"
  }
}
