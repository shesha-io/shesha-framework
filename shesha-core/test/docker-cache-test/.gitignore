# Local SQL credentials -- never commit. Covers .env and any variant (.env.local-sql etc).
.env
.env.*
# ...but the template is meant to be committed.
!.env.example

# Published application output, regenerated by publish.ps1.
publish/
